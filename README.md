# Employee Manager (angularai workspace)

This is a small React + TypeScript app (Vite) to manage employees in-browser using `localStorage`.

Quick start:

```bash
npm install
npm run dev
```

Open the URL shown by Vite (usually http://localhost:5173).
